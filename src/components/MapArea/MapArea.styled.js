import styled from "styled-components";

const Container = styled.div`
  display: flex;
  flex-direction: row;
`;

const RvWrapper = styled.div`
  flex: 1;
`;

const MapWrapper = styled.div`
  flex: 1;
`;
export default { Container, RvWrapper, MapWrapper };
